#!/bin/bash
#cmd arg: <jar> <Xmx>
#env arg: [JAVA_BIN]

export OMSLS_OPTS="$OMSLS_OPTS"

if [[ "$OMSLS_GC_FLAGS" == "" ]]; then
  export OMSLS_GC_FLAGS=$(cd $(dirname $0); pwd)/flags/zgc.txt
fi

omsls17 $1 $2
